name: AWS and GITHUB

on:
  push:
  workflow_dispatch:

permissions:
      id-token: write
      contents: read

jobs:
  callerIdentity:
    runs-on: ubuntu-22.04
    environment: dev
    uses: alphaveer/docker-awscli/.github/workflows/terraform.yaml@master
